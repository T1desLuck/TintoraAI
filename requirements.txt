# Основные зависимости

# PyTorch стек (ядро обучения)
torch==2.3.1                 # Основной фреймворк глубинного обучения (Tensor/Autograd/CUDA)
torchvision==0.18.1          # Доп. утилиты и модели для CV (трансформации, датасеты, ops)

# Численные вычисления и конфиги
numpy<2.0                    # Базовые численные операции (массива/векторы), совместимость <2.0
PyYAML>=6.0.1                # Парсинг YAML-конфигов (configs/*.yaml)

# Утилиты и прогресс-бар
tqdm>=4.66.0                 # Прогресс-бары в консоли/ноутбуках

# Обработка изображений и научные пакеты
opencv-python>=4.9.0         # Чтение/запись изображений, базовые CV-операции (I/O, resize, цветовые модели)
scipy>=1.11.0                # Научные функции и численные алгоритмы (интерполяции, фильтры)
scikit-image>=0.22.0         # Доп. инструменты CV (фильтры, метрики, преобразования)

# Работа с тензорами и дифференцируемым CV
einops>=0.7.0                # Удобная перестановка осей/reshape (читаемые паттерны)
kornia>=0.7.2                # Дифференцируемые CV-операции на PyTorch (геометрия/фильтры/аугментации)

# Логирование и визуализация
tensorboard>=2.16.0          # Логи обучения: скаляры, изображения, графы (TensorBoard)
matplotlib>=3.8.0            # Построение графиков/визуализаций
pillow>=10.2.0               # Работа с изображениями (PIL) — чтение/запись/преобразования

# Модели и слои (timm)
timm>=0.9.16                  # Коллекция SOTA‑моделей/блоков (ConvNeXt, CoAtNet и др.)

# Необязательные метрики и логирование
DISTS-pytorch>=0.1.0          # Метрика DISTS (перцептуальное/структурное сходство), замена LPIPS
colorama>=0.4.6               # Цветной вывод в консоль (удобочитаемые логи)

# Инструменты для разработки (необязательно)
# Тесты и качество кода
pytest>=8.2.0                 # Тестовый фреймворк (запуск тестов из каталога tests/)
black>=24.3.0                 # Форматтер кода (единый стиль Python)
ruff>=0.4.0                   # Быстрый линтер (PEP8/ошибки/подсказки)
isort>=5.13.2                 # Сортировка импортов (группы/алфавит)
mypy>=1.8.0                   # Статическая типизация (проверка типов по аннотациям)

# Примечание: на Windows pip автоматически подтянет подходящие колёса (wheels) CUDA 12.1 для torch 2.3.1/torchvision 0.18.1.