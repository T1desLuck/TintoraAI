# Workflow для Python проекта: установка зависимостей, линтинг, тесты и загрузка coverage и результатов тестов в Codecov
name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # Скачиваем репозиторий
    - uses: actions/checkout@v4

    # Устанавливаем Python
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"

    # Устанавливаем зависимости
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest pytest-cov
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    # Линтинг кода
    - name: Lint with flake8
      run: |
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        flake8 . --count --exit-zero --max-complexity=10 --statistics

    # Запуск тестов с генерацией coverage и JUnit XML
    - name: Test with pytest, coverage and JUnit
      run: |
        pytest --cov=. --cov-report=xml --junitxml=junit.xml -o junit_family=legacy

    # Загрузка coverage в Codecov
    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: coverage.xml
        fail_ci_if_error: true

    # Загрузка результатов тестов для Test Analytics
    - name: Upload test results to Codecov
      if: ${{ !cancelled() }}
      uses: codecov/test-results-action@v1
      with:
        token: ${{ secrets.CODECOV_TOKEN }}